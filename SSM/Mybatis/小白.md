           
### 1.Mybatis的概念

mybatis是作用在三层架构中的第三层（数据访问层也叫持久层），由于JDBC代码中存在重复，所以为了更好的使用推进了mybatis技术。

### 2.ORM
#### 2.1.ORM的概念

```html
1.ORM思想：
	O（Object）:表示java中的对象
	R（Relational）:表示关系型数据库
	M（Mapping）:映射
	
在关系型数据库中用表来记实录数据,
那么对应到java中则是一个类，

表中的每一个字段，对应类中的属性
表中的每一行数据，对应java中的每一个对象，
所以关系型数据库中的表和java中的对象可以相互转换
这就是ORM思想

```

![](图片/ORM.png)


### 3.单元测试 junit

```
// 名字规范：你要测试的类名+Test  
// 一般是一个业务方法对应一个测试方法  
// 测试方法的规范： public void testXxxx(){}//测试方法的方法名以test开始，假设测试的方法是sum，这个测试方法名：testSum

每一个测试方法的上方必须要引入一个@Test
```

**测试方法中有两个值：**

1.实际值：被测试的业务方法真正执行结果
2.期望值：执行了这个业务方法之后，你期望的执行结果是多少
那么我们在执行的时候要将这两个值进行断言：==Assert.assertEquals()==
```java
public void testSum(){  
    MathService mathService = new MathService();  
    int actual = mathService.sum(1, 2);  
    int expected = 3;  
    Assert.assertEquals(expected,actual);  
}
```

![java截图](024.png)

### 4.集成日志

#### 4.1 mybatis常见的集成日志组件都有哪些？
```
1.SLF4J（沙拉风）
2.LOG4J
3.lOG4J2
4.STDOUT_LOGGING

其中STDOUT_LOGGING是标准日志，mybatis已经实现了这种标准日志。mybatis框架本身已经实现了这种标准。
只要开启就行，开启的方法：
<!--    settings:控制 MyBatis 的默认行为（缓存、命名规则、懒加载、日志等）-->  
    <settings>  
        <setting name="logImpl" value="STDOUT_LOGGING"/>  
    </settings>
注意事项：
settings标签的位置必须要写在environments标签上面，具体位置参考dtd文件
如果想要使用更丰富的配置，可以集成第三方log组件

集成logback日志框架
	logback日志框架实现了slf4j标准 --->日志中的门面
	第一步：引入logback的依赖
	第二步：引入logback所必须的xml配置文件
		这个配置文件的名字必须叫做：logback.xml或者logback-test.xml,不能是其他名字
		这个配置文件必须放在根路径下，不能放在其他位置 
如果用的是STDOUT_LOGGING日志，必须要配置settings，如果要配置第三方日志，则可以不配置settings.
```

#### 4.2 slf4j的配置
1.引入依赖
![引入logback依赖](028.png)
2.编写logback.xml文件
<span style="color:orange">
注意事项：
1.logback.xml还可以叫logback-test.xml,但只能叫这连个名字
2.logback.xml必须放在根据路径下
</span>

![logback.xml](029.png)


### 5.Mybatis工具类的编写

```
1.工具类的构造方法一般是私有化的
private xxxUtil(){}
2.工具类中所有的方法都是静态的，直接采用类名即可调用，不用创建对象。

```

由于每个environment配置一个sql版本，所以只需要new 一次SqlSessionFactory就可以了。那么我们就可以根据这一特征进行编写工具类。

```java
public class sqlSessionUtil {  
    //私有化构造方法  
    private sqlSessionUtil(){}  
    // 成员方法都是静态的  
  
    //将SqlSessionFactory设为静态的  
    static SqlSessionFactory sqlSessionFactory;  
  
    static {  
        try {  
            sqlSessionFactory = new SqlSessionFactoryBuilder().build(Resources.getResourceAsStream("mybatis-config.xml"));  
        } catch (IOException e) {  
            throw new RuntimeException(e);  
        }  
    }  
  
    public static SqlSession openSession(){  
        return sqlSessionFactory.openSession();  
    }  
}
```

![工具类|652](030.png)
![调用工具类|665](032.png)











#  ==开发经验==

##  resources目录的细节

1. 放在resources目录当中的，一般都是资源文件，配置文件。直接放到resources目录下的资源，等同于放到了类的根路径下。

## 从XML中构建SqlSessionFactory

1. 在Mybatis中一定有一个很重要的对象，这个对象是：SqlSessionFactory对象。

2. SqlSessionFactory对象的创建需要XML

3. XML是什么
	它是一个配置文件

4. mybatis中有两个主要的配置文件

==mybatis-config.xml和xxxxMapper.xml==

	其中一个是：mybatis-config.xml，这是核心配置文件，主要配置连接数据库的信息等。(一般只有一个表)

	另一个是：xxxxMapper.xml，这个文件是专门用来编写SQL语句的配置文件(一张表一个)


## SqlSessionFactoryBuilder、sqlSessionFactory、sqlSession的获取步骤细节


在编写代码中，先获取的是SqlSessionFactoryBuilder对象，
再通过SqlSessionFactoryBuilder对象的build()方法获取sqlSessionFactory对象，
那么在使用build方法的时候，形参需要传递的是一个输入流，所以先创建一个输入流，
那么我们使用Resources类中的getResourceAsStream()方法获取输入流，
getResourceAsStream()形参传递的则是mybatis-config.xml的地址，获取sqlSessionsFactory
通过sqlSessionFactory对象中的openSession()方法获取sqlSession

```
/*  
*   sqlSessionFactoryBuilder.build(InputStream InputStream),  
*   由于build方法中的参数是一个InputStream对象，所以我们要先创建一个InputStream对象  
*   来读取MyBatis的核心配置文件mybatis-config.xml  
*   我们可以利用Resources类来获取InputStream对象，  
*   Resources类中有一个静态方法getResourceAsStream(String str)  
*   这个方法的参数是一个字符串，表示要读取的资源文件的路径，默认是从类的根路径下开始查找资源  
* */
  
```

## 执行sql语句的细节

通过sqlSession.insert()方法来执行sql语句，insert()方法的形参是要==执行的语句的id==,在xxxxMapper.xml文件中配置

![](017.png)

