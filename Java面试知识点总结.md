# Java 面试知识点总结（由浅入深）

> 定位：初级 60% + 中级 40%。每题不只给结论，尽量补充"为什么"，方便理解记忆而不是死背。

---

## 一、Java 基础语法 / 面向对象

### 1. 面向对象三大特性：封装、继承、多态分别是什么？
- **封装**：把对象的属性和方法包装起来，通过 `private` 隐藏内部实现细节，只暴露必要的 `public` 方法（getter/setter）供外部访问。
  - 为什么要这样做：外部代码只依赖"接口"而不依赖"内部实现"，将来改内部实现（比如把 `ArrayList` 换成 `LinkedList`）不会影响调用方；同时可以在 setter 里加校验逻辑，防止属性被设置成非法值。
- **继承**：子类通过 `extends` 复用父类的属性和方法，实现代码复用，同时可以对父类方法进行扩展或重写。Java 只支持**单继承**（一个类只能有一个直接父类），但可以通过实现多个接口达到类似多继承的效果。
  - 为什么不支持多继承：如果两个父类有同名方法，子类不知道该继承哪一个，会产生"菱形继承"的二义性问题；接口默认方法虽然可以多实现，但 Java 要求子类必须显式重写来解决冲突，从语言层面规避了歧义。
- **多态**：同一个方法调用，作用在不同对象上产生不同的行为。体现为：**编译时多态**（方法重载 Overload）和**运行时多态**（方法重写 Override，父类引用指向子类对象）。
  - 好处：调用方只需面向父类/接口编程，不需要关心具体是哪个子类实现，新增一种子类实现时调用方代码完全不用改，符合"开闭原则"。

### 2. 多态的底层实现原理是什么？
- Java 运行时多态基于**动态绑定（Dynamic Binding）**：JVM 为每个类维护一张**虚方法表（vtable）**，表中记录了该类可调用的方法及其实际入口地址。
- 调用一个实例方法时（`invokevirtual` 指令），JVM 不是在编译期确定调用哪个方法，而是在运行时根据对象的**实际类型**去查找虚方法表，找到对应的重写方法执行——这就是"多态"能够生效的底层原因。
- 反过来，`private`、`static`、`final` 方法和构造方法**不能被重写**，因为它们在编译期就能唯一确定调用目标，属于**静态绑定**，不需要走虚方法表查找。

### 3. Java 有哪些访问修饰符？区别是什么？
| 修饰符 | 同类 | 同包 | 子类（不同包） | 不同包非子类 |
|---|---|---|---|---|
| `private` | ✔ | ✘ | ✘ | ✘ |
| default（不写） | ✔ | ✔ | ✘ | ✘ |
| `protected` | ✔ | ✔ | ✔ | ✘ |
| `public` | ✔ | ✔ | ✔ | ✔ |

- 设计原则：成员变量尽量用 `private`，通过 `public` 方法暴露访问入口。原因：如果字段是 `public` 的，外部可以绕过任何校验直接改值，一旦以后要加校验逻辑或改变内部表示（比如把 `int age` 改成 `LocalDate birthday`），所有直接访问字段的外部代码都要跟着改，破坏了封装的初衷。

### 4. static 关键字的作用？
- 修饰**变量**：类变量（静态变量），被该类所有实例共享，随类加载而初始化，只有一份内存。适合存放"所有对象共有"的数据，比如计数器、常量。
- 修饰**方法**：静态方法，属于类而非对象，可以直接通过类名调用。因为静态方法在类加载时就已经确定，不依赖任何实例，所以**不能访问非静态成员**（此时可能还没有任何实例存在，也就没有非静态成员可言）。
- 修饰**代码块**：静态代码块，在类加载时执行且只执行一次，常用于静态资源初始化（如加载配置文件、初始化连接池）。
- 修饰**内部类**：静态内部类不持有外部类实例的引用，可以独立 `new`；而非静态内部类会隐式持有外部类的引用（原理是编译器自动在内部类中加了一个指向外部类的字段），因此必须先有外部类实例才能创建。
- 本质：`static` 成员存放在方法区（元空间），是类级别的共享数据，而不是对象级别的数据。

### 5. this 和 super 关键字的区别？
- `this`：指向**当前对象**的引用。常见用途：
  - 区分成员变量和同名的局部变量/参数（如 `this.name = name`）；
  - 在一个构造方法里调用本类的另一个构造方法（`this(...)`，必须是第一行），避免重复写初始化逻辑；
  - 把当前对象作为参数传递给别的方法（比如注册监听器时传 `this`）。
- `super`：指向**父类**部分。常见用途：
  - 调用父类的构造方法（`super(...)`，必须是子类构造方法第一行）——因为子类对象在初始化自己的字段之前，必须先保证父类那部分已经初始化完毕，所以强制放在第一行；
  - 访问被子类重写/覆盖的父类方法或被隐藏的父类字段（比如子类重写了 `toString()`，但仍想在里面调用父类原来的逻辑，就用 `super.toString()`）。

### 6. 类的构造方法/代码块的执行顺序是怎样的？
执行顺序（存在继承关系时）：
1. 父类静态代码块 / 静态变量初始化（只在类第一次加载时执行一次）
2. 子类静态代码块 / 静态变量初始化
3. 父类实例代码块 / 实例变量初始化
4. 父类构造方法
5. 子类实例代码块 / 实例变量初始化
6. 子类构造方法

- 为什么是这个顺序：静态内容只和"类"绑定，跟创建了多少个对象无关，所以在类加载阶段一次性执行完，且一定早于任何对象的创建；而实例部分每次 `new` 都要重新走一遍，并且父类的实例状态必须先准备好，子类构造方法里才能安全地使用父类提供的字段和方法（这也是为什么 `super(...)` 必须写在子类构造方法第一行）。
- 一句话总结：**静态的先于非静态的，父类先于子类，代码块先于构造方法**。

### 7. `==` 和 `equals()` 有什么区别？
- `==`：比较基本类型时比较的是**值**；比较引用类型时比较的是**内存地址（是否是同一个对象）**，因为引用类型变量里存的本身就是一个地址，`==` 只是简单比较这两个地址是不是完全一样。
- `equals()`：是 `Object` 类定义的一个**方法**，可以被子类重写自定义比较逻辑。`Object` 类里默认实现其实就是 `return this == obj`，也就是没重写时 `equals()` 和 `==` 效果一样。但 `String`、`Integer` 等包装类重写了它，改成了逐个比较内容（比如 `String` 的 `equals()` 会遍历比较每一个字符）。
- 结论：如果需要判断两个对象"内容相同"而不是"同一个对象"，要么用已经重写过 `equals()` 的类（如 `String`），要么自己重写 `equals()`。

### 8. 为什么重写 `equals()` 必须重写 `hashCode()`？
- Java 规范（`Object` 类文档）规定的**契约**：如果两个对象通过 `equals()` 判断相等，那它们的 `hashCode()` 必须返回相同的值（反过来不要求：哈希值相同的两个对象不一定 equals 相等，这就是"哈希碰撞"）。
- 为什么会有这个要求——根源在于 `HashMap`/`HashSet` 这类基于哈希表的集合的查找逻辑：存入元素时先算 `hashCode()`，决定放进哪个桶（bucket）；查找/判断是否包含某个元素时，也是先用 `hashCode()` 定位到对应的桶，再在桶内逐个用 `equals()` 比对，确认是不是真的是那个元素。
- 如果只重写了 `equals()`（内容相同即判定相等）却没重写 `hashCode()`（还在用默认的地址哈希），就会出现："两个内容一样的对象，`equals()` 认为它们相等，但因为 `hashCode()` 不同，会被分到不同的桶里"——结果就是 `set.contains(obj)` 明明有一个内容一样的元素在集合里，却返回 `false`；或者往 `HashSet` 里放"内容重复"的对象时没有被去重，出现了两份逻辑上相同的数据。
- 记忆口诀：**equals 相等 → hashCode 一定相等；hashCode 相等 → equals 不一定相等**。

### 9. Integer 缓存（`==` 比较包装类的坑）是什么？
- `Integer` 内部有一个 `IntegerCache`，在类加载时会预先创建好 `-128 ~ 127` 这个区间内所有的 `Integer` 对象缓存起来（这是 JDK 为了节省内存做的优化，因为这个区间的小整数在程序里被频繁使用）。
- 通过 `Integer.valueOf(int)`（自动装箱底层调用的就是这个方法）创建对象时，如果值落在缓存区间内，会**直接返回缓存中已有的对象**，而不是 new 一个新对象；超出这个区间，才会真正 `new Integer(...)`。
- 因此 `Integer a = 100; Integer b = 100;`：两者其实指向的是同一个缓存对象，`==` 比较为 `true`；而 `Integer a = 200; Integer b = 200;`：两次都各自 new 了新对象，`==` 比较为 `false`。这是面试代码陷阱高频考点，实际开发中比较包装类的值应统一用 `equals()`，不要用 `==`。

### 10. 自动装箱/拆箱（Autoboxing/Unboxing）是什么？有什么坑？
- 装箱：基本类型自动转换为对应包装类，如 `Integer i = 10;`，编译器在背后自动改写成 `Integer.valueOf(10)`。
- 拆箱：包装类自动转换为基本类型，如 `int j = i;`，编译器自动改写成 `i.intValue()`。
- 坑1：包装类对象如果是 `null`，一旦参与需要拆箱的运算（比如 `int x = someInteger + 1`），会先自动调用 `.intValue()`，但对 `null` 调用方法必然抛出 `NullPointerException`，而且这个错误发生的位置很隐蔽，不易一眼看出。
- 坑2：如果在循环里频繁用包装类做运算（比如 `Long sum = 0L; for (...) sum += i;`），因为每次运算都要先拆箱再装箱，超出缓存范围时每次都会 new 出一个新对象，产生大量临时垃圾对象，增加 GC 压力——这也是为什么性能敏感的累加场景要用基本类型而不是包装类。

### 11. String 为什么是不可变的（immutable）？
- 底层用 `final` 修饰字符数组（JDK9 前是 `char[]`，JDK9 后是 `byte[] + coder` 编码标识，用来区分是 Latin-1 还是 UTF-16 编码，从而节省内存），并且这个数组一旦赋值，`String` 类内部没有任何方法可以修改它的内容——所有看似"修改字符串"的方法（如 `substring`、`replace`、`+` 拼接）实际上都是**返回一个新的 String 对象**，原对象不受影响。
- 不可变带来的好处（也是"为什么要设计成不可变"的原因）：
  - **线程安全**：多个线程共享同一个 `String` 对象时不会有并发修改的问题，天然线程安全。
  - **可以安全地缓存哈希值**：`String` 重写了 `hashCode()`，因为内容不会变，第一次计算后可以把结果缓存在字段里，之后直接返回缓存值，不用每次重新计算——这让 `String` 特别适合作为 `HashMap` 的 key。
  - **支持字符串常量池复用**：因为不可变，多个变量可以安全地共享指向同一个字符串对象，不用担心一处修改影响到其他引用它的地方，从而节省内存。

### 12. String、StringBuilder、StringBuffer 的区别？
| | 可变性 | 线程安全 | 性能 |
|---|---|---|---|
| String | 不可变 | 安全（天然） | 频繁拼接性能差 |
| StringBuilder | 可变 | 不安全 | 高，单线程场景首选 |
| StringBuffer | 可变 | 安全（方法加了 synchronized） | 略低于 StringBuilder |

- 为什么 String 拼接性能差：前面说过 `String` 是不可变的，每次用 `+` 拼接都会生成一个新的 `String` 对象，原来的旧对象变成垃圾等待回收；如果在循环里做大量字符串拼接，会产生大量临时对象，既浪费内存又拖慢速度。
- `StringBuilder`/`StringBuffer` 内部维护一个**可变的字符数组**，拼接操作是在原数组基础上直接追加（数组不够时才扩容拷贝），避免了每次拼接都创建新对象，所以效率高得多。
- `StringBuffer` 的每个方法都加了 `synchronized`，保证多线程同时操作时不会出错，但加锁本身有性能开销；单线程场景下这个开销是纯浪费，所以官方更推荐用没有加锁的 `StringBuilder`。

### 13. `new String("a")` 和 `"a"`（字符串字面量）有什么区别？
- `"a"` 这种字面量写法，JVM 在编译期就会把它放进**字符串常量池**（如果池里已经有相同内容的字符串，直接复用，不会重复创建）。
- `new String("a")` 显式使用了 `new` 关键字，一定会在**堆**中创建一个新对象（即使常量池里已经有内容相同的 `"a"`，也不会复用，而是把常量池里的这个字符串对象内容拷贝一份到堆里新建对象）。因此 `new String("a") == "a"` 结果是 `false`——两者是不同的对象，只是内容相同。
- 可以用 `intern()` 方法手动把堆中字符串"放入"（如果不存在）或者"关联到"（如果已存在）常量池中的引用，之后再用 `==` 比较就会是 `true`。

### 14. 重载（Overload）和重写（Override）的区别？
- **重载**：发生在**同一个类**中，方法名相同，参数列表（类型/个数/顺序）不同，和返回值类型、访问修饰符无关。因为参数列表在编译期就能确定，所以调用哪个重载方法是**编译期**就决定好的，属于"静态多态"。
- **重写**：发生在**父子类**之间，子类重写父类方法时要求方法签名（方法名+参数列表）完全相同，访问权限不能比父类更小，返回值类型要相同或是父类返回类型的子类（协变返回类型）。因为具体调用哪个方法要看运行时对象的真实类型才能确定（参见第2题的虚方法表），所以属于**运行期**决定，是"动态多态"。
- 简单区分记忆：重载看"参数长得不一样"，重写看"父子类方法长得一样但执行内容不一样"。

### 15. 接口（interface）和抽象类（abstract class）的区别？
- 抽象类可以有构造方法、普通成员变量、非抽象方法；一个类只能继承一个抽象类（Java 单继承的限制）。适合表达"是什么"的关系（比如 `Animal` 抽象类，`Dog`/`Cat` 继承它，共享一部分已经实现好的通用逻辑）。
- 接口在 JDK8 之前只能有抽象方法和常量（隐式 `public static final`）；JDK8 起可以有 `default`/`static` 方法（主要是为了让接口能够新增方法而不破坏已有的实现类——否则接口一旦发布，任何新增的抽象方法都会导致所有实现类编译报错），JDK9 起还可以有 `private` 方法（用来让多个 `default` 方法之间复用代码，同时不暴露给外部）。一个类可以实现多个接口，弥补了单继承的局限。
- 一句话区分：接口体现的是"能做什么"（一种行为约定/能力标签），抽象类体现的是"是什么"（一种血缘关系，常配合模板方法模式，把公共流程写好、留几个步骤给子类实现）。

### 16. 值传递还是引用传递？
- Java **只有值传递**，没有引用传递。
- 传基本类型时，传的是值本身的一份拷贝，方法内部改这个拷贝完全不影响外部原变量。
- 传对象时，传的是**"引用"这个值**的拷贝——也就是说，拷贝的是"指向对象的地址"，而不是对象本身。这带来一个容易搞混的现象：在方法内部通过这个引用去修改对象的属性（比如 `obj.setName("x")`），因为拷贝的引用和外部引用指向的是**同一个对象**，所以外部看得到这个修改；但如果在方法内部把这个引用**重新指向一个新对象**（比如 `obj = new Xxx()`），只是改变了拷贝本身指向哪里，外部的原始引用依然指向原来的对象，不受影响。这正是"传的是值（引用的值），不是引用本身"的体现。

### 17. final、finally、finalize 的区别？
- `final`：修饰**类**表示不可被继承；修饰**方法**表示不可被子类重写（常用于禁止修改核心逻辑，同时因为编译期就能确定不会被重写，JVM 可以对其做内联优化）；修饰**变量**表示只能赋值一次（基本类型值不可变，引用类型则是引用本身不能再指向别的对象，但对象内部状态依然可以改）。
- `finally`：`try-catch` 语句中用来存放"无论是否发生异常都必须执行"的清理代码（如关闭文件流、释放数据库连接），保证资源不会因为异常而泄漏；即使 `try`/`catch` 里有 `return`，`finally` 也会在方法真正返回给调用者之前执行完。唯一的例外是在此之前调用了 `System.exit()` 直接终止了 JVM 进程。
- `finalize`：`Object` 类定义的一个方法，本意是给对象一个"临终前"做清理工作的机会（在对象即将被 GC 回收前调用）。但因为 GC 什么时候执行本身是不确定的，`finalize` 什么时候被调用、甚至会不会被调用都无法保证，还可能拖慢 GC、导致对象复活等问题，所以 JDK9 起已经被标记为过时（deprecated），实际开发中应该用 `try-with-resources` 或显式调用 `close()` 来管理资源，而不是依赖 `finalize`。

### 18. Java 有哪几种内部类？
- **成员内部类**：定义在类内部（和成员变量同级），会隐式持有一份指向外部类实例的引用（编译器在背后自动加了这个字段），所以必须先有外部类的实例，才能创建成员内部类的实例（`outer.new Inner()`）。
- **静态内部类**：用 `static` 修饰，不持有外部类引用，可以独立 `new`，不依赖外部类实例。常用于构建器模式（如 `Xxx.Builder`），因为构建器本身不需要访问外部类的实例状态。
- **局部内部类**：定义在方法体内部，作用域仅限该方法，用完即释放，使用场景较少。
- **匿名内部类**：没有类名，通常用于临时实现一个接口或抽象类（比如给按钮注册一个一次性的点击监听器）。JDK8 引入 Lambda 表达式后，很多"只有一个抽象方法"的匿名内部类场景已经被 Lambda 取代，写法更简洁。

### 19. 什么是泛型？什么是类型擦除？
- 泛型：让类/接口/方法在定义时不写死具体类型，而是用类型参数（如 `T`）占位，使用时再指定具体类型（如 `List<String>`）。好处是把原本要在**运行时**才能发现的类型错误（比如把 `Integer` 塞进本该装 `String` 的集合，运行时才 `ClassCastException`），提前到**编译期**就能被发现，同时不用写强制类型转换代码。
- 类型擦除：Java 的泛型是**伪泛型**——这些类型参数信息只存在于源码和编译期检查阶段，编译成字节码后就被"擦除"了。也就是说 `List<String>` 和 `List<Integer>` 编译后在字节码层面其实都变成了原始类型 `List`，JVM 运行时根本不知道这个 `List` 装的是什么类型，必要时编译器会在取值的地方自动插入强制类型转换代码。
- 为什么要设计成类型擦除而不是像 C++ 模板那样真正生成多份代码：主要是为了兼容 JDK5 之前编写的没有泛型的旧代码，让泛型可以平滑引入而不破坏已有的字节码和类库。

### 20. 反射是什么？有什么优缺点？
- 反射：Java 程序在**运行时**（而不是编译时）动态地获取一个类的信息（有哪些属性、方法、构造器等）并操作对象——比如不知道类名字符串对应的类具体长什么样，也能在运行时把它加载进来、创建实例、调用方法。核心类是 `Class`、`Method`、`Field`、`Constructor`。
- 常见用途：框架底层大量依赖反射，比如 Spring 通过反射读取配置/注解，动态创建 Bean 并完成依赖注入；MyBatis 通过反射把数据库查询结果映射到 Java 对象的字段上；JSON 序列化框架通过反射遍历对象的字段来生成 JSON。
- 缺点：
  - **性能低于直接调用**：反射调用方法/字段时需要做很多额外的安全检查、动态解析目标方法，比直接写代码调用慢不少（虽然 JIT 和缓存机制已经优化了很多）。
  - **破坏封装性**：可以通过 `field.setAccessible(true)` 绕过 `private` 限制直接访问私有成员，这在带来灵活性的同时也带来了安全隐患。

### 21. 注解（Annotation）是什么？常见元注解有哪些？
- 注解本质上是一种特殊的接口（继承自 `Annotation`），用来给代码（类、方法、字段等）打上一段"元数据"标签。注解本身**不会直接影响程序的运行逻辑**，必须配合"读取这个注解的代码"（编译时注解处理器，或运行时反射）才能真正发挥作用。
- 常见元注解（用来注解"注解"本身的注解）：
  - `@Retention`：控制注解的生命周期——`SOURCE`（只在源码中存在，编译后丢弃，如 `@Override`）、`CLASS`（保留到字节码文件但运行时不加载，默认值）、`RUNTIME`（运行时依然保留，可以通过反射读取，Spring 的注解大多是这一类）。
  - `@Target`：限定这个注解能标注在什么地方（方法/类/字段/参数等）。
  - `@Documented`：标注了它的注解会被包含进 javadoc 文档。
  - `@Inherited`：标注了它的注解，如果用在父类上，子类会自动"继承"这个注解。
- 典型应用：Spring 的 `@Autowired`（自动装配）、`@Component`（声明一个 Bean），Lombok 的 `@Data`（编译期自动生成 getter/setter 等样板代码）。

---

## 二、集合框架

### 22. ArrayList 和 LinkedList 的区别？
- `ArrayList`：底层是一个可扩容的数组。因为数组支持通过下标直接定位内存地址，随机访问（按下标取值）是 O(1)；但在数组中间插入/删除元素时，需要把该位置之后的元素整体往前/往后挪一位，是 O(n)。默认初始容量 10，容量不够时扩容为原来的 1.5 倍（并拷贝旧数据到新数组）。
- `LinkedList`：底层是双向链表，每个节点存着数据和前后节点的引用。查找第 n 个元素必须从头（或从尾，取更近的一端）依次往下找，是 O(n)；但如果已经拿到了某个节点的引用，在它前后插入/删除只需要改几个指针的指向，是 O(1)。此外 `LinkedList` 还实现了 `Deque` 接口，可以当队列或栈使用。
- 选型建议：查询多、增删少 → `ArrayList`；频繁在两端或已知位置增删 → `LinkedList`（不过实际工程中 `ArrayList` 使用频率远高于 `LinkedList`，因为现代 CPU 对数组的连续内存访问更友好，缓存命中率更高）。

### 23. HashMap 的底层实现原理？（高频必考）
- JDK8 之前：数组 + 链表——数组的每个位置（桶）存一个链表头，哈希冲突的元素挂在同一个链表上。
- JDK8 之后：数组 + 链表 + 红黑树。当**链表长度 ≥ 8 且数组长度 ≥ 64** 时，链表会转成红黑树。原因是：链表查找是 O(n)，如果哈希冲突严重、某个桶下挂了很多元素，查找效率会明显下降；转成红黑树后查找是 O(log n)，能兜底极端情况下的性能。之所以要求数组长度也 ≥ 64 才转树，是因为数组较小时更应该优先扩容（扩容能直接把冲突打散到更多桶里），而不是急着转成结构更复杂、内存占用更大的红黑树。
- 存储流程：调用 `put(key, value)` 时先计算 `key` 的哈希值 `hash(key)`，再通过 `(n-1) & hash` 定位到数组的哪个下标（`n` 为数组长度）——用位运算代替取模，是因为当 `n` 是 2 的幂时，`(n-1) & hash` 和 `hash % n` 结果完全一样，但位运算比取模运算快得多。

### 24. HashMap 为什么要用扰动函数？
- 扰动函数：`hash = h ^ (h >>> 16)`，其中 `h` 是 key 原始的 `hashCode()`。这一步让 hashCode 的**高 16 位**也参与到最终定位数组下标的运算中。
- 为什么需要它：前面提到定位下标用的是 `(n-1) & hash`，而实际开发中数组长度 `n` 通常不会特别大（比如默认 16），`(n-1)` 的二进制形式只有低几位是 1，也就是说最终只有 hashCode 的**低位**在参与决定落在哪个桶——如果很多 key 的 hashCode 低位相同、只有高位不同（这种情况并不罕见），不做处理的话这些 key 会全部堆到同一个桶里，退化成链表甚至频繁触发树化，性能变差。
- 扰动函数把高位信息"异或"下来跟低位混合，让高低位共同决定最终落在哪个桶，使得哈希分布更均匀，减少哈希冲突。

### 25. HashMap 的扩容机制是怎样的？
- 默认负载因子（load factor）是 0.75，当元素数量超过 `容量 × 负载因子`（这个阈值叫 threshold）时触发扩容，容量直接翻倍（保持 2 的幂，这是为了让 `(n-1) & hash` 这种位运算定位下标的技巧依然成立）。
- 为什么负载因子是 0.75 而不是 1（用满数组容量再扩容）：如果等到数组完全存满才扩容，链表会变得很长，哈希冲突严重，查询效率明显下降；0.75 是在"空间利用率"和"冲突概率"之间做的一个经验性折中，是官方经过统计测试给出的推荐值。
- 扩容具体做法：JDK8 里有个巧妙的优化——因为容量永远是 2 的幂，一个已有节点在扩容之后（容量翻倍）的新位置，只有两种可能：**还在原来的下标**，或者**原下标 + 旧容量**。判断方法是看这个 key 的 hash 值在"旧容量"那一位上是 0 还是 1（`hash & oldCap`）。这样不需要像 JDK7 那样重新完整计算哈希，只需一次位运算判断，就能把旧链表里的节点分成两组，分别放到新数组的两个位置，效率更高。

### 26. HashMap 为什么线程不安全？
- JDK7：多线程同时触发扩容时，链表的迁移采用**头插法**——多个线程交叉执行迁移逻辑时，可能导致节点之间的 `next` 指针相互指向对方，形成一个**环形链表**。之后一旦有线程遍历到这个环，会陷入死循环，永远走不出来（表现为 CPU 占用飙升到 100%）。
- JDK8：把迁移逻辑改成了**尾插法**，扩容后节点的相对顺序保持不变，避免了成环的问题。但即便如此，多线程并发场景下仍然可能出现**数据覆盖/丢失**——比如两个线程同时执行 `put`，都计算好了要往同一个桶插入新节点，但因为没有加锁保护，后写入的线程可能会覆盖掉先写入线程刚放进去的节点，导致一次 `put` 操作"凭空消失"。
- 结论：`HashMap` 本身完全没有做任何同步措施，只是"恰好在单线程下表现正确"；多线程场景下应该用专门为并发设计的 `ConcurrentHashMap`，而不是给 `HashMap` 的方法自己加锁（那样等于自己重新实现一遍同步逻辑，还不一定做得对）。

### 27. ConcurrentHashMap 如何保证线程安全？
- JDK7：采用**分段锁（Segment）**——把整个 `HashMap` 拆分成若干个 `Segment`（每个 `Segment` 本身就是一个小号的、继承自 `ReentrantLock` 的 HashMap），不同的 `Segment` 之间可以并发操作，只有同一个 `Segment` 内的操作才需要互相等锁。这样把锁的粒度从"整个 Map"缩小到"某一段"，提高了并发度。
- JDK8：干脆去掉了 `Segment` 这一层，改用 **`synchronized` + `CAS`**：`put` 一个新 key 到空桶时用 CAS 无锁完成；如果桶已经有数据（发生冲突），才对这个桶的**链表头节点**（或红黑树的根节点）加 `synchronized` 锁。这样锁粒度进一步细化到了单个桶，不同桶之间完全不互相阻塞，并发性能比 JDK7 的分段锁更好。

### 28. HashMap、HashTable、TreeMap 的区别？
- `HashTable`：早期 JDK 提供的线程安全 Map，靠给几乎所有方法加 `synchronized` 来实现同步，粒度是整个对象，并发度很低；而且**不允许 null key/value**（源码里对 null 会直接抛异常）。因为性能差且历史包袱较重，现在基本被 `ConcurrentHashMap` 取代，不建议在新代码里使用。
- `HashMap`：线程不安全，但允许一个 `null` key 和多个 `null` value，因为没有任何同步开销，效率明显高于 `HashTable`，是单线程/已知安全场景下的首选。
- `TreeMap`：底层基于红黑树实现，所有 key 会按照自然顺序（或构造时传入的自定义 `Comparator`）保持有序，插入/查找/删除都是 O(log n)。实现了 `NavigableMap` 接口，提供了诸如"找比某个 key 大的最小 key"这类范围查询方法，适合需要有序遍历或范围查询的场景。

### 29. 什么是 fail-fast 机制？
- Java 集合（如 `ArrayList`、`HashMap`）内部维护了一个 `modCount` 字段，记录这个集合被结构性修改（增加/删除元素，注意单纯的 `set` 赋值不算）的次数。
- 用迭代器（`Iterator`）遍历集合时，迭代器内部会记住"遍历开始那一刻"的 `modCount`；每次调用 `next()` 都会检查当前集合的 `modCount` 是否和自己记住的值一致，如果不一致（说明遍历过程中集合被别的方式改动过），就会立刻抛出 `ConcurrentModificationException`，而不是让程序继续在一个"状态已经不一致"的集合上悄悄跑出错误结果——这就是"fail-fast"（快速失败）的含义。
- 一个常见的坑：在增强 for 循环（底层就是用迭代器）里直接调用集合本身的 `remove()` 方法删元素，会导致 `modCount` 变化但迭代器不知情，下一次 `next()` 就会抛异常。正确的删除方式是使用 `Iterator.remove()`——这个方法内部会同步更新迭代器自己记住的 `modCount`，从而避免检测冲突。

---

## 三、并发编程

### 30. 创建线程有哪几种方式？
1. 继承 `Thread` 类，重写 `run()`：简单直接，但 Java 单继承的限制下，这个类不能再继承别的类。
2. 实现 `Runnable` 接口：只需要实现 `run()` 方法，再把这个 `Runnable` 对象交给 `Thread` 去执行，避免了单继承的局限，是更推荐的方式。
3. 实现 `Callable` 接口 + `FutureTask`：和 `Runnable` 的区别是 `call()` 方法**可以有返回值**、可以**抛出受检异常**，配合 `FutureTask` 可以在需要的时候通过 `get()` 阻塞获取执行结果。
4. 通过线程池创建（实际工程中最推荐）：直接 `new Thread()` 会带来线程创建和销毁的开销，高并发场景下频繁创建销毁线程会严重拖累性能；线程池提前创建好一批线程并复用，避免了这部分开销，同时能统一管理线程数量、防止无限制创建线程耗尽系统资源。

### 31. Java 线程有哪些状态？
- `NEW`（新建，`Thread` 对象已创建但还没调用 `start()`）
- `RUNNABLE`（可运行/运行中，调用了 `start()` 后，正在等待 CPU 调度或正在执行）
- `BLOCKED`（阻塞，正在等待获取一把被别的线程占用的锁）
- `WAITING`（无限期等待，比如调用了不带超时参数的 `wait()`、`join()`，需要别的线程唤醒或结束才能继续）
- `TIMED_WAITING`（限时等待，比如 `sleep(time)`、带超时参数的 `wait(time)`，时间到了会自动恢复）
- `TERMINATED`（终止，`run()` 方法执行完毕或抛出未捕获异常导致线程结束）

### 32. sleep() 和 wait() 的区别？
- `sleep()`：是 `Thread` 类的**静态方法**，单纯让当前线程暂停执行一段时间，**不会释放它持有的锁**（如果调用 `sleep` 时正持有某把锁，其他线程在这段时间内依然拿不到这把锁）。时间一到自动恢复为可运行状态。
- `wait()`：是 `Object` 类的方法，必须在获得对象锁（即在 `synchronized` 块/方法内）的情况下才能调用，否则会抛 `IllegalMonitorStateException`。调用后会**释放当前持有的锁**，让其他线程有机会进来获取这把锁执行别的逻辑；需要别的线程调用同一个对象的 `notify()`/`notifyAll()`，或者到达超时时间，才会被唤醒重新去竞争锁。
- 这个区别的本质原因：`sleep` 只是单纯的"歇一会儿"，跟锁和线程间协作没有关系；`wait`/`notify` 是专门为了线程间协作设计的一套机制（比如经典的生产者-消费者模型：消费者发现队列为空就 `wait()` 并让出锁，生产者放入数据后 `notify()` 唤醒消费者），所以必须配合锁一起使用。

### 33. synchronized 的底层原理？
- 修饰代码块：编译后对应 `monitorenter`/`monitorexit` 这两条字节码指令，线程执行到 `monitorenter` 时尝试获取对象的监视器锁（monitor），执行完代码块到 `monitorexit` 时释放锁。
- 修饰方法：不是通过字节码指令实现，而是给方法本身打上 `ACC_SYNCHRONIZED` 标志，JVM 在调用这个方法时会检查这个标志，自动在方法调用前后完成加锁解锁，效果和给整个方法体包一层同步代码块类似。
- 对象头中有一块叫 `Mark Word` 的区域，专门存储和锁相关的状态信息。JDK6 之后引入了锁升级机制来优化性能：**无锁 → 偏向锁 → 轻量级锁 → 重量级锁**。
  - 为什么要分这么多级：大多数情况下锁其实并没有真正的多线程竞争（同一个线程反复获取同一把锁的情况很常见），如果每次加锁都直接走"挂起等待"这种重量级操作，开销太大。所以先尝试**偏向锁**（记住是哪个线程持有，同一线程下次来直接免检查通过）；如果发现有其他线程也来competing，升级成**轻量级锁**（用 CAS 自旋等待，避免线程真正被挂起）；如果自旋还是拿不到（竞争激烈），才最终升级成**重量级锁**（真正把线程挂起，让出 CPU，等锁释放后由操作系统唤醒）。这个过程一般是单向不可逆的，因为把资源消耗低的锁"降级"回去的场景很少见，收益不划算。

### 34. volatile 关键字的作用？
- 保证**可见性**：一个线程修改了被 `volatile` 修饰的变量后，这个新值会立即被强制刷新到主内存；其他线程读取这个变量时，也会被强制去主内存重新读最新值，而不是使用自己 CPU 缓存里的旧值——这就避免了"一个线程改了值，另一个线程一直看不到"的问题。
- 保证**有序性**：编译器和 CPU 为了优化性能，有时会对没有依赖关系的指令重新排序；`volatile` 通过在读写它的地方插入**内存屏障**，禁止编译器和 CPU 把 `volatile` 读写前后的指令重新排序穿插过去，从而保证程序执行顺序和代码书写顺序一致。
- **不保证原子性**——这是最容易考的坑点：像 `i++` 这种看起来是一步操作，实际上分为"读取 i → 加 1 → 写回 i"三步。即使 `i` 被 `volatile` 修饰，也只能保证每一步读写时看到的是最新值，但没法保证这三步作为一个整体不被其他线程插入进来打断（比如两个线程同时读到了 i=5，都各自加 1 后写回 6，实际上应该是 7）。要解决这类复合操作的原子性问题，需要用 `synchronized` 或者专门的原子类（如 `AtomicInteger`，内部基于 CAS 实现）。

### 35. 什么是 JMM（Java 内存模型）？什么是 happens-before？
- JMM（Java Memory Model）定义了 Java 程序中，各个线程如何通过"内存"这个媒介进行交互——具体来说，规定了共享变量什么时候对其他线程可见、指令重排序在多大范围内是被允许的，从而让"并发程序在不同硬件平台上表现一致"这件事有了统一的规范可依。
- `happens-before` 是 JMM 用来描述"两个操作之间是否存在可以保证的先后依赖关系"的一套规则——如果操作 A happens-before 操作 B，意味着 A 的执行结果对 B 可见，且 A 在时间上先于 B 发生（编译器/CPU 不能把这个顺序打乱）。常见规则：
  - 程序顺序规则：同一线程内，代码书写在前面的操作 happens-before 后面的操作（注意这只保证"单线程内看起来"顺序执行，不代表实际底层没有指令重排，只是重排后的结果对单线程而言表现得跟顺序执行一样）。
  - volatile 变量规则：对一个 volatile 变量的写操作，happens-before 后续对这个变量的读操作。
  - 锁规则：一个线程对某把锁的解锁操作，happens-before 后续另一个线程对同一把锁的加锁操作。
  - 传递性：如果 A happens-before B，且 B happens-before C，那么 A happens-before C。
- 这些规则的意义在于：写并发代码时不需要去死记硬背底层内存屏障的实现细节，只要按照 happens-before 规则去组织代码（比如用同一把锁保护共享变量），就能保证多线程下的可见性和正确性。

### 36. synchronized 和 ReentrantLock 的区别？
- `synchronized` 是 Java **语言层面的关键字**，由 JVM 在字节码层面负责自动加锁和释放锁（哪怕代码块内抛了异常，锁也会自动释放），使用起来简单，不容易忘记解锁。
- `ReentrantLock` 是 **JUC 并发包提供的一个类**，需要开发者显式调用 `lock()` 获取锁、`unlock()` 释放锁（且 `unlock()` 必须写在 `finally` 块里，否则一旦中间抛异常，锁就永远不会被释放，造成死锁隐患）。
- `ReentrantLock` 相比 `synchronized` 更灵活，多出了几项能力：
  - **可中断**获取锁：`lockInterruptibly()`，等待锁的过程中可以响应中断，不用死等；
  - **可设置超时**：`tryLock(timeout)`，等待一段时间还拿不到锁就放弃，避免无限期阻塞；
  - **可以实现公平锁**：构造时传入 `true` 表示公平锁，让等待时间最长的线程优先获取锁，而 `synchronized` 只能是非公平的；
  - **可以绑定多个 `Condition`**：一把锁可以对应多个等待队列，实现更精细的线程唤醒（比如生产者和消费者可以用不同的 `Condition`，避免像 `wait/notifyAll` 那样"全体线程都被唤醒再抢"的低效场景）。
- 两者都支持**可重入**（同一个线程可以多次获取同一把锁而不会自己把自己锁死）。需要说明的是，JDK6 以后 `synchronized` 经过锁升级等优化，性能已经和 `ReentrantLock` 相差不大，所以选择依据主要看是否需要上面这几项额外功能，而不是单纯比性能。

### 37. 什么是 AQS（AbstractQueuedSynchronizer）？
- AQS 是 Java 并发包的一个核心基础框架，像 `ReentrantLock`、`CountDownLatch`、`Semaphore`、`ReentrantReadWriteLock` 这些常用的同步工具，底层都是基于它实现的——换句话说，AQS 把"排队等待、获取/释放资源"这套通用逻辑抽象了出来，不同的同步工具只需要在这个框架上定义"什么叫获取成功""什么叫释放成功"，就能快速实现出一个新的同步器，避免了每个工具都要重新写一遍加锁排队的逻辑。
- 核心结构：
  - 一个 `volatile int state`，用来表示当前的同步状态（比如对 `ReentrantLock` 来说，`state` 就是这把锁被重入的次数，0 表示未加锁）；
  - 一个 **FIFO 双向等待队列**（CLH 队列的变体），线程如果尝试获取资源失败（比如锁已被别人占用），就会被封装成一个 `Node` 节点加入这个队列并挂起等待；等资源被释放时，会从队列头部唤醒下一个等待的线程去竞争。
- 这样设计的好处：`state` 用 CAS 保证了修改的原子性，配合队列实现了"没抢到就排队，而不是无脑自旋浪费 CPU"的高效等待机制。

### 38. 线程池的核心参数有哪些？
`corePoolSize`（核心线程数，正常情况下始终保留的线程数量）、`maximumPoolSize`（最大线程数，任务多到一定程度时最多能扩到多少线程）、`keepAliveTime`（超出核心线程数的那部分空闲线程，多久没任务就会被回收）、`unit`（`keepAliveTime` 的时间单位）、`workQueue`（核心线程都在忙时，新任务排队等待的队列）、`threadFactory`（用来自定义创建线程的方式，比如给线程命名方便排查问题）、`handler`（当线程数和队列都已经用满时，如何处理新提交进来的任务，即拒绝策略）。

### 39. 线程池提交任务的执行流程？
1. 核心线程数未满 → 直接创建一个新的核心线程来执行这个任务（哪怕此时有别的核心线程正空闲着，也会优先创建新线程，而不是复用——这是线程池的默认策略，为的是尽快把核心线程数配满，保证响应速度）。
2. 核心线程数已满，队列未满 → 新任务进入队列排队，等待现有线程忙完后来处理。
3. 队列已满，线程数还没达到 `maximumPoolSize` → 创建"非核心"线程来处理，帮忙应对突发流量。
4. 队列已满且线程数也已达到最大值 → 说明系统已经饱和，触发**拒绝策略**，按照配置好的方式处理这个多出来的任务（而不是无限制接收导致系统被压垮）。

### 40. 线程池有哪几种拒绝策略？
- `AbortPolicy`：直接抛出 `RejectedExecutionException` 异常，让调用方知道任务被拒绝了（**默认策略**）。
- `CallerRunsPolicy`：不抛异常也不丢弃，而是让"提交任务的那个线程自己去执行"这个任务——这样做能起到一种天然的"限流"效果，因为提交者被占用去跑任务了，短时间内没法继续提交新任务，给线程池一个喘息、消化积压的机会。
- `DiscardPolicy`：直接静默丢弃这个任务，什么反馈都没有，适合"丢了也无所谓"的非关键任务。
- `DiscardOldestPolicy`：把队列里排得最久的那个任务丢弃，然后把当前这个新任务重新尝试提交进去，相当于"优先处理新任务"。

### 41. 为什么阿里巴巴开发手册不推荐用 Executors 创建线程池？
- `Executors.newFixedThreadPool()`、`newCachedThreadPool()` 等快捷方法底层默认使用 `LinkedBlockingQueue` 作为任务队列，而这个队列如果不显式指定容量，**默认容量是 `Integer.MAX_VALUE`**，相当于一个几乎无限长的队列。
- 问题在于：一旦任务提交速度长期大于处理速度，队列会持续堆积任务而不会触发拒绝策略（因为在拒绝策略生效之前，队列"永远还没满"），最终可能把内存耗尽，抛出 `OutOfMemoryError`，而且这个问题往往在线上压力突增时才会暴露，排查起来很麻烦。
- 因此推荐手动 `new ThreadPoolExecutor(...)`，显式指定一个有界队列的容量，这样队列一旦真的堆满就会立刻按照配置的拒绝策略处理，把"资源耗尽的风险"提前变成一个"编码阶段就能看到、必须明确处理"的问题，而不是留到生产环境才爆发。

### 42. 什么是 CAS？有什么问题？
- CAS（Compare And Swap，比较并交换）是一种**乐观锁**思想的实现：操作时带上三个值——内存地址 V、期望的旧值 A、要更新成的新值 B。CPU 会原子性地判断"V 当前的值是不是还等于 A"，如果是，就把它更新成 B；如果不是（说明这段时间被别的线程改过了），就更新失败，通常配合一个循环不断重试（自旋），直到成功为止。它是通过 `Unsafe` 类的 native 方法实现的，依赖 CPU 提供的原子指令支持。
- 相比 `synchronized` 这种"悲观锁"（先加锁再操作，没抢到锁的线程直接被挂起等待），CAS 属于乐观锁——它假设大多数情况下不会发生冲突，先尝试操作，冲突了再重试，避免了线程被挂起/唤醒的开销，在竞争不激烈的场景下性能更好。
- **ABA 问题**：CAS 只检查"当前值是否还等于期望的旧值 A"，但如果这段时间里这个值被别的线程从 A 改成了 B，又改回了 A，CAS 检测时会发现"值还是 A"，误以为没有被修改过，从而错误地认为可以安全更新——但实际上中间已经发生过变化，如果这个变化对业务逻辑有影响（比如链表节点被换过），就会出问题。解决方案：使用 `AtomicStampedReference`，除了比较值本身，还额外附带一个版本号/时间戳，每次修改都递增这个版本号，这样即使值变回了 A，版本号也已经不同，能够被准确检测出来。

---

## 四、JVM

### 43. Java 运行时内存区域分为哪几部分？
- **线程私有**（每个线程独立拥有一份，互不干扰）：
  - 虚拟机栈：每个方法调用会在栈里压入一个"栈帧"，存放这个方法的局部变量、操作数栈等信息，方法执行完出栈——这也是为什么局部变量不需要加锁也天然线程安全的原因，因为每个线程操作的是自己独立的栈。
  - 本地方法栈：和虚拟机栈类似，只不过是为执行 native（本地）方法服务的。
  - 程序计数器：记录当前线程执行到了字节码的哪一行，线程切换后恢复执行时，靠它知道该从哪里继续。
- **线程共享**（所有线程共用同一份，因此涉及并发访问，需要考虑线程安全）：
  - 堆：存放几乎所有的对象实例，是垃圾回收（GC）关注的主要区域。
  - 方法区/元空间：JDK8 之后用**元空间（Metaspace）**替代了之前的"永久代"，用来存放类信息、常量、静态变量等。之所以要用元空间替代永久代，主要是因为永久代属于堆的一部分、大小受 `-XX:MaxPermSize` 限制且很容易因为动态生成大量类（比如某些框架大量使用动态代理）而导致 OOM；元空间则改为使用**本地内存**（不占用堆空间），默认情况下容量只受本机可用内存限制，大大降低了这类 OOM 的概率。

### 44. 如何判断一个对象是否可以被回收？
- **引用计数法**：给每个对象附加一个计数器，被一个地方引用就加 1，引用失效就减 1，计数器归零就判定可以回收。这个方法实现简单，但**无法解决循环引用**的问题——比如对象 A 引用对象 B，对象 B 又引用对象 A，即使外部已经没有任何地方再引用这两个对象了，它们互相之间的引用计数依然不为 0，导致永远不会被回收，造成内存泄漏。因为这个致命缺陷，Java 没有采用这种方式。
- **可达性分析法**（Java 实际采用的方式）：从一系列被称为 `GC Roots` 的起点对象出发（包括：虚拟机栈中正在被引用的对象、方法区中静态属性引用的对象、常量引用的对象等），沿着引用关系向下搜索，走过的路径称为"引用链"。如果一个对象**没有任何引用链能够到达它**，就判定它是可以被回收的垃圾——这种方式天然就能正确处理循环引用的情况：即使 A 和 B 互相引用，只要它们和 GC Roots 之间没有任何路径能连通，照样会被判定为可回收，一起被清理掉。

### 45. Java 的垃圾回收算法有哪些？
- **标记-清除**：先从 GC Roots 出发标记所有存活对象，然后把所有没被标记的（也就是垃圾）对象直接清除。实现简单，但清除之后内存中会留下大量不连续的空闲碎片——如果之后要分配一个较大的对象，即使空闲内存总量足够，也可能因为没有一块连续够大的空间而提前触发下一次 GC，甚至分配失败。
- **标记-整理**：在标记-清除的基础上多了一步——把所有存活对象往内存的一端整体挪动、压缩到一起，再清理掉边界之外的所有空间。这样清理完之后内存是**连续的、没有碎片**的，但因为要移动对象（还要同步更新所有指向这些对象的引用），成本比单纯标记-清除更高。常用于**老年代**（老年代对象存活率高，直接清除会产生大量碎片，所以更适合用整理的方式）。
- **复制算法**：把可用内存分成两块，每次只使用其中一块；GC 时把这一块里还存活的对象，全部复制到另一块空闲的里面去，然后把原来那一整块直接清空。因为只需要顺序复制存活对象，效率很高，且没有碎片问题；缺点是可用内存直接减半，比较浪费空间——但这个缺点在**新生代**（大部分对象"朝生夕死"，存活率很低，需要复制的对象很少）里影响不大，所以复制算法常用于新生代。

### 46. 什么是分代收集？新生代/老年代是怎么划分的？
- 分代收集是基于一个统计规律得出的经验性设计：绝大多数对象存活时间都很短（"朝生夕死"），只有少数对象会长期存活。既然新对象和老对象的存活规律差别很大，就没必要用同一种算法、同一种回收频率去对待所有对象，于是把堆划分成不同的"代"，针对每一代对象的存活特点选用最合适的回收算法（呼应第45题：新生代用复制算法、老年代用标记-整理）。
- 具体划分为**新生代**（又细分为 Eden 区和 2 块 Survivor 区，默认比例 8:1:1）和**老年代**。
- 对象一般优先在 Eden 区分配；Eden 区满触发一次 Minor GC 时，还存活的对象会被复制到其中一块 Survivor 区；之后再触发 GC，会在两块 Survivor 区之间来回复制，并给每个对象的"年龄"计数器加一；如果一个对象经历了足够多次 GC 依然存活（默认阈值是 15 次），就会被认为是"长期存活对象"，晋升到老年代。另外体积特别大的对象也可能直接分配进老年代，跳过新生代，避免在新生代里来回复制大对象浪费性能。

### 47. 常见的垃圾回收器有哪些？
- `Serial`：单线程执行垃圾回收，GC 时会暂停所有用户线程（Stop The World）。虽然听起来落后，但因为没有线程切换开销，在内存不大、单核/资源有限的场景（如客户端应用、容器内小内存实例）反而效率不低。
- `ParNew`：可以理解为 `Serial` 的多线程版本，用多条线程并行执行垃圾回收，常配合 CMS 一起使用（负责新生代回收）。
- `CMS`（Concurrent Mark Sweep）：以尽可能缩短用户线程停顿时间为设计目标，核心特点是回收线程可以和用户线程**并发**执行（大部分阶段不需要完全暂停用户线程），响应速度好；缺点是基于标记-清除算法，会产生内存碎片，而且和用户线程并发运行会占用一部分 CPU 资源。因为这些缺点以及被 G1 全面取代，CMS 已在较新版本的 JDK 中被移除。
- `G1`（Garbage First）：把整个堆划分成很多个大小相等的独立小区域（Region），不再要求新生代、老年代在物理空间上连续；每次优先回收"垃圾最多、回收收益最大"的那些 Region（这也是 G1 名字的由来）。它可以让用户设定一个期望的最大停顿时间目标，G1 会尽量按这个目标去控制每次回收的 Region 数量，兼顾了吞吐量和低延迟，是 JDK9 起的**默认**垃圾回收器。

### 48. Minor GC、Major GC、Full GC 有什么区别？
- **Minor GC**：只回收**新生代**，触发条件通常是 Eden 区空间不够用了。因为新生代对象存活率低、每次能清理的空间比例很大，所以 Minor GC 触发很频繁，但每次耗时通常很短。
- **Major GC / Full GC**：回收整个堆（包括新生代、老年代，通常也会顺带清理方法区/元空间）。因为要扫描的对象数量多、老年代对象存活率高（复制/标记开销大），触发频率远低于 Minor GC，但每次耗时长得多，通常伴随较长时间的 STW（Stop The World，所有用户线程暂停）。
- 实际调优的一个重要目标就是尽量避免触发 Full GC（或者拉长两次 Full GC 之间的间隔），比如通过合理设置堆大小、及时排查内存泄漏、避免创建过多长生命周期的大对象，来减少 Full GC 对系统响应时间的影响。

### 49. 类加载的过程是怎样的？什么是双亲委派模型？
- 类加载过程：**加载 → 验证 → 准备 → 解析 → 初始化 → 使用 → 卸载**。简单说：加载是把 `.class` 文件读进内存；验证确保字节码内容符合规范、不会危害 JVM 安全；准备阶段给类的静态变量分配内存并赋"默认值"（如 0、null，还不是代码里写的初始值）；解析把常量池里的符号引用转换成直接引用；初始化阶段才真正执行静态代码块和给静态变量赋"代码里写的值"。
- 双亲委派模型：一个类加载器收到"帮我加载某个类"的请求时，**不会自己先尝试加载**，而是先把这个请求向上委派给自己的父加载器，父加载器也会继续往上委派，一直到最顶层的启动类加载器；只有当父加载器（包括一路往上所有的父加载器）都表示"我加载不了/没有这个类"时，当前这个子加载器才会自己去尝试加载。
- 为什么要这样设计：这样可以确保像 `java.lang.String` 这种 JDK 核心类，无论是哪个业务代码触发的加载请求，最终都会被委派到最顶层同一个加载器去加载，保证了同一个类在整个程序中只有唯一的一份定义——如果没有这个机制，恶意代码可以自己定义一个假的 `java.lang.String` 类去替换掉真正的核心类库，造成安全隐患。
- 打破双亲委派的场景：自定义类加载器（如 Tomcat 需要让不同 Web 应用之间的同名类互相隔离，不能都委派给同一个父加载器加载）、SPI 机制（接口定义在启动类加载器能加载到的核心库里，但具体实现类往往在业务代码的类路径下，只能通过线程上下文类加载器反过来去加载，绕开了严格的双亲委派）、热部署（需要在不重启整个 JVM 的情况下替换某个类的实现，只能通过自定义加载器重新加载）。

---

## 五、异常处理

### 50. Java 异常体系结构是怎样的？
- 顶层是 `Throwable`，分为两大分支：
  - `Error`：表示 JVM 自身或系统级别的严重问题（如 `OutOfMemoryError` 内存耗尽、`StackOverflowError` 栈溢出），这类问题往往意味着程序已经处于无法自行恢复的状态，所以一般不建议、也没必要去 `catch` 它们。
  - `Exception`：表示程序运行过程中可以预期、并且理应被处理的问题，又分为两类：
    - **受检异常**（Checked Exception，`RuntimeException` 之外的 `Exception`，如 `IOException`）：编译器会强制要求必须显式处理（用 `try-catch` 捕获，或者在方法签名上用 `throws` 声明抛给调用方），否则编译不通过。这类异常通常代表"外部环境导致的、调用方理应能预见并处理"的情况，比如文件不存在、网络中断。
    - **非受检异常**（`RuntimeException` 及其子类，如 `NullPointerException`、`ArrayIndexOutOfBoundsException`）：编译器不强制处理。这类异常通常代表"代码逻辑本身的错误"，理论上应该通过修正代码来避免，而不是到处写 `try-catch` 掩盖问题。

### 51. try-catch-finally 中，如果 catch 里有 return，finally 还会执行吗？
- 会执行。`finally` 块的设计初衷就是"无论 try/catch 里发生什么（正常返回、抛出异常、甚至 return），都要保证这段清理代码一定跑一遍"，常用来关闭文件流、释放数据库连接等，避免因为异常导致资源没被正确释放而泄漏。唯一的例外是执行到 `System.exit()` 直接终止了整个 JVM 进程，或者 JVM 本身崩溃。
- 执行顺序上，`finally` 是在 `return` **真正把值返回给调用者之前**插入执行的——可以理解成：`try`/`catch` 里的 `return xxx` 会先把返回值计算好、临时存起来，然后去执行 `finally`，`finally` 跑完之后才真正把这个值返回出去。
- 需要特别注意的坑：如果 `finally` 块里**也写了** `return`，它会**直接覆盖掉** `try`/`catch` 中原本准备返回的值——因为 `finally` 里的 `return` 是最后执行的，会把之前"临时存起来的返回值"整个替换掉。这种写法虽然合法，但会让代码逻辑变得难以理解，实际开发中应避免在 `finally` 里写 `return`。

---

## 六、JDK 新特性（近几年高频）

### 52. Lambda 表达式和函数式接口是什么？（JDK8）
- 函数式接口：一个只包含**唯一一个抽象方法**的接口（可以有 `default`/`static` 方法，但那些不算），比如 `Runnable`（只有一个 `run()`）、`Comparator`（核心是 `compare()`）。可以用 `@FunctionalInterface` 注解标注，编译器会帮忙检查这个接口是不是真的只有一个抽象方法。
- Lambda 表达式是专门为函数式接口设计的一种简洁写法，比如 `(a, b) -> a + b` 可以直接当作一个 `(int, int) -> int` 类型函数式接口的实现传进去，省掉了以前用匿名内部类那一大堆样板代码（`new Comparator<Integer>() { public int compare(...) {...} }`）。
- 实现原理上，Lambda 并不是简单编译成一个匿名内部类，而是借助 JDK7 引入的 `invokedynamic` 字节码指令和方法句柄（MethodHandle），在真正第一次调用时才动态生成具体的实现类，相比传统匿名内部类，避免了一些不必要的类加载开销，性能通常更好。

### 53. Stream API 的常用操作有哪些？
- 中间操作（**惰性求值**，只是记录下"要做什么"，不会立即真正执行）：`map`（把每个元素转换成另一种形式）、`filter`（按条件过滤）、`sorted`（排序）、`distinct`（去重）、`limit`/`skip`（截取/跳过）。
- 终端操作（真正触发遍历和计算）：`collect`（收集成集合）、`forEach`（逐个处理）、`reduce`（把所有元素归约成一个结果，比如求和）、`count`（计数）、`anyMatch`/`allMatch`（判断是否有/所有元素满足条件）。
- 为什么中间操作要设计成惰性求值：这样 Stream 可以把多个中间操作"合并"在一次遍历里完成（而不是每个 `map`/`filter` 都单独遍历一次整个集合），只有遇到终端操作时才会真正按需、逐元素地把整条操作链跑一遍，效率更高，也支持处理理论上无限长的流。
- 需要注意：一个 Stream 对象只能被消费一次（调用过一次终端操作后就"用完"了），如果对同一个 Stream 再调用第二次终端操作会直接抛 `IllegalStateException`，需要重新从数据源生成一个新的 Stream。

### 54. Optional 是用来解决什么问题的？
- 用来更优雅地表达和处理"这个值可能不存在"的情况，取代过去那种"返回 null，调用方必须自己记得判空，一旦忘了判就是 `NullPointerException`"的老模式。`Optional<T>` 本质是一个包装容器，要么装着一个具体的值，要么是"空"。
- 通过把"可能没有值"这件事显式体现在方法的返回类型上（`Optional<User>` 而不是 `User`），可以促使调用方在写代码时就意识到要处理"空"的情况，而不是等到运行时才踩坑。
- 常用方法：`Optional.ofNullable(obj)`（把一个可能为 null 的对象包装成 Optional）、`isPresent()`（判断是否有值）、`orElse(default)`（有值就返回值，没有就返回一个默认值）、`orElseGet(supplier)`（和 `orElse` 类似，但默认值是"需要时才去计算"，避免不必要的开销）、`map()`（对内部的值做转换，如果本身是空则直接跳过转换）。

### 55. Record 是什么？（JDK16 正式）
- `record` 是专门用来快速定义"不可变数据载体类"（比如常见的 DTO、VO，只是单纯用来装数据传来传去，没有什么业务逻辑）的一种语法糖。只要声明 `record Point(int x, int y) {}`，编译器就会自动生成：包含这些字段的构造方法、每个字段对应的（无 get 前缀的）访问方法（如 `x()`、`y()`）、以及基于所有字段实现好的 `equals()`、`hashCode()`、`toString()`。
- 为什么会有这个特性：在 `record` 出现之前，定义一个这样的简单数据类需要手写一大堆样板代码（字段、构造方法、getter、`equals`/`hashCode`/`toString`），容易出错也很啰嗦（虽然 Lombok 等第三方库早就能自动生成，但 `record` 是 Java 语言原生内置的方案，不需要额外依赖）。`record` 生成的字段默认是 `final` 的，天然不可变，也更符合"数据载体应该尽量设计成不可变对象"这一并发友好的最佳实践。

### 56. Sealed Class（密封类）是什么？（JDK17）
- 通过 `sealed` 关键字定义一个类或接口，并用 `permits` 明确列出"只允许哪几个类去继承/实现它"，除了这个白名单里的类，其他任何类都不能继承它——相当于在"完全开放继承"和"用 `final` 完全禁止继承"之间，多了一种"半开放、可控继承"的选择。
- 为什么需要它：假设一个业务场景里，某个类型理论上只应该有几种确定的子类型（比如一个"形状"类只应该是圆形、方形、三角形之一），过去用普通继承没法在语言层面限制别人再写一个新的子类，也就没法保证这个"有限的类型集合"假设一直成立。用 `sealed` 声明后，配合 `switch` 的模式匹配语法，编译器**知道所有可能的子类型有哪些**，可以在写 `switch` 分支时检查是不是漏掉了某个子类型的处理（"穷尽性检查"），如果漏了会在编译期直接报错提醒，而不是等到运行时才发现某个分支没处理导致的 bug。

---

*小贴士：面试时不要只背答案，建议结合自己的项目（如高并发秒杀、缓存穿透/雪崩等场景）说明"你在哪里用到了这个知识点、当时是怎么想的"，会比单纯背诵更有说服力。*
