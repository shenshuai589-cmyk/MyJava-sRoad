1. 局部代码块（在main方法里面)
	public static void main(String[] args){
		{
			int a = 10；
				System.out.println(a);
			}
			当代码执行到“ } ”时，变量a消失
	}
	2. 构造代码块（写在成员位置的代码块)
		public class Animal( ){
			private String name;
			private int age;
			{
				System.out.printIn("开始创建了");
			}
			public Animal( ){
			
			}
			public Animal(String name,int age){
				this.name = name;
				this.age = age;
			}
		}
		先执行构造代码块，再执行构造方法
	