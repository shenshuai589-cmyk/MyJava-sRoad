
### 写入文件（write）

```python
with open("filename.txt", "w", encoding="utf-8") as f:
	
	f.write("hello world") # 注意，此时还未写入到硬盘中，而是写在了当前程序的缓冲区当中
	
	f.flush() # 将内存中记载的内容刷新到硬盘中
	
	f.close() # close文件内置了flush功能了
```