> 为了让 key 更有结构、更易读，Redis 提供了一种**通过分隔符实现逻辑分层**的经典设计

### 层级结构设计：使用冒号:

>Redis 的 key 允许使用冒号 **`:`** 来划分层次。这类似于 Windows 的文件夹或者 Java 的包名结构。

行业标准命名规范；
`[项目名]:[业务模块]:[数据类型/唯一标识]`

set powernode:user:1: '{"id":1, "name":"鲜于灿", "age":20}'

